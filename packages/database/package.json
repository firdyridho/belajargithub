{
  "name": "@tiss/database",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo database lint skipped",
    "type-check": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "echo database tests skipped",
    "clean": "rimraf dist .turbo"
  },
  "devDependencies": {
    "@tiss/typescript-config": "workspace:*",
    "typescript": "^5.6.0"
  }
}
